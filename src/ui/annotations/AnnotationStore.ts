import { useAnnotationStore } from '../../store/annotationStore';

/**
 * Zustand-backed annotation store bridge for the annotation UI components.
 * Re-exports the store for use in annotation components.
 */
export { useAnnotationStore as AnnotationStore };
