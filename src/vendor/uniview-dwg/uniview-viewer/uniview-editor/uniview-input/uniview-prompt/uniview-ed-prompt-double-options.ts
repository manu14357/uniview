import { UvEdPromptNumericalOptions } from './uniview-ed-prompt-numerical-options'

/**
 * Options for prompting the user to enter a double-precision (floating point) number,
 * similar to AutoCAD .NET `PromptDoubleOptions`.
 *
 * Inherits common numeric-prompt behavior from `UvEdPromptNumericalOptions`.
 */
export class UvEdPromptDoubleOptions extends UvEdPromptNumericalOptions {
  // For now there are no new memebers needed
}
